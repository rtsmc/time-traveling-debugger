# Python Debugger Package

This directory contains the active Python time-traveling debugger implementation.

The repository README is located one directory up:

```text
../README.md
```

Run project commands from this directory:

```bash
uv sync
uv run make
uv run python idebug.py ../TestingFiles/test_clean_simple.py
```
